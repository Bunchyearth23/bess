# MVP verification — September 22, 2026

## Observed results

- `cargo test --release -- --nocapture`: 7 passing tests (5 unit, 2 integration).
- `cargo clippy --all-targets -- -D warnings` passed.
- Release build succeeded; executable copied to `dist/BESS.exe`.
- SHA256: `5FD6E0C02EA476C1257A92A8FE159802540E0FD1B8381756542BF69D84B56444`.
- A one-second offline render took about 16 ms in the final test; this is neither hardware latency nor a real-time guarantee.
- Demo WAV: `output/bess-demo.wav`, 10 seconds, mono 48 kHz / 24 bit.
- CPAL stream opened on the Sound BlasterX G6 at 192 kHz stereo; 200 silent callbacks in two seconds with no reported error. See `audio-check.txt`.
- BESS window started; accessibility tree showed presets, settings, play/stop, save/open/export and device controls.
- Real ZIP inspection succeeded: thunderhawk_zero, 56 WAVs, one blend, 800 idle, 12,000 maximum, cylinder count absent from inspected field. See `vehicle-check.txt`.

## Precise limits

- Windows Graphics Capture failed with `SetIsBorderRequired failed` and unsupported interface `0x80004002`. Accessibility was readable, but automated input failed with `coordinate input geometry is unavailable` and `foreground window did not report a process id`. No captured visual validation or complete user workflow was claimed.
- Subjective listening and an extended session with rapid changes remained. Hardware output was not recorded.
- JBeam inspection tolerated omitted commas through a lexical reader that skipped comments and strings. It did not interpret active parts/configurations.
- There were no seamless loops or BeamNG audio package yet and no in-game proof.

## Test coverage

Finite, bounded, non-silent output for four presets and four rpm points; repeatability and stop fade; cancellation of harmonic orders incompatible with four evenly spaced firings; JSON round trip and invalid version/value rejection; WAV size, format, duration and signal; rejection of metadata occurring only inside comments, strings or expressions.
